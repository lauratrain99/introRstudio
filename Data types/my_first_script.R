# Code to compute the solution to quadratic equation of the form
# ax^2 + bx + c. Define the variables
a <- 1
b <- 1
c <- -1

# now compute the solution
(-b + sqrt(b^2 - 4*a*c)) / (2*a)
(-b - sqrt(b^2 - 4*a*c)) / (2*a)

